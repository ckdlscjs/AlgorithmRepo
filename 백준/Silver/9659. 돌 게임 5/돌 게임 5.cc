#include <bits/stdc++.h>
using namespace std;
long long int N;
int main() 
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    std::cin >> N;
    std::cout << (N % 2 ? "SK" : "CY");
    return 0;
}
